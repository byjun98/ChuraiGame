# 🎮 GameMatch (ChuraiGame) - 프로젝트 발표 자료

> **"당신의 취향, AI가 찾아드립니다."**  
> Steam 연동 기반 하이브리드 게임 추천 & AI 큐레이팅 플랫폼

---

## 📋 목차

1. [주제](#1-주제)
2. [주제 선정 이유 및 소개](#2-주제-선정-이유-및-소개)
3. [사용 기술 및 설계](#3-사용-기술-및-설계)
4. [AI 기능 상세](#4-ai-기능-상세)
5. [구현 내용](#5-구현-내용)
6. [주요 코드 및 알고리즘](#6-주요-코드-및-알고리즘)
7. [ERD 관계](#7-erd-관계)
8. [어려웠던 점 및 해결 과정](#8-어려웠던-점-및-해결-과정)
9. [역할 분담 및 일정](#9-역할-분담-및-일정)
10. [시연](#10-시연)

---

## 1. 주제

### 🎯 프로젝트명: GameMatch (내부명: ChuraiGame)

**한 줄 소개:** Steam 계정 연동 기반의 AI 게임 추천 플랫폼

**핵심 기능:**
- 🔗 **Steam OpenID 연동** - 사용자의 게임 라이브러리 자동 분석
- 🤖 **AI 게임 큐레이터** - GPT-5 Nano 기반 맞춤 추천 챗봇
- 📊 **하이브리드 추천 알고리즘** - Item-Based CF + Content-Based Filtering
- 🌐 **다국어 지원** - Gemini 2.0 Flash Lite 기반 실시간 번역

---

## 2. 주제 선정 이유 및 소개

### 💡 문제 정의

> **"할 게임이 없어요"** - 수많은 게이머들의 공통된 고민

**현재 게임 추천 시스템의 문제점:**
1. **단순 인기순 추천** - 개인 취향 미반영
2. **낮은 정확도** - 유저의 실제 플레이 패턴 무시
3. **정보 과부하** - Steam 16만+ 게임 중 선택 어려움
4. **Cold Start 문제** - 신규 유저 추천 불가

### 🎯 솔루션: GameMatch

| 문제 | 해결책 |
|------|--------|
| 개인화 부족 | Steam 라이브러리 + 플레이타임 분석 |
| Cold Start | 왓챠 스타일 온보딩 (스와이프 평가) |
| 정보 과부하 | AI 큐레이터와 대화형 추천 |
| 언어 장벽 | Gemini 실시간 한국어 번역 |

### 🏆 핵심 가치

```
┌──────────────────────────────────────────────────────────┐
│  🔗 Connectivity   │  Steam 계정 연동 한 번으로 분석      │
│  🎯 Personalization│  플레이타임 + 평가 데이터 결합       │
│  ⚡ Optimization   │  DB 캐싱으로 98% 속도 향상          │
│  🤖 Intelligence   │  GPT 기반 AI 큐레이터 대화          │
└──────────────────────────────────────────────────────────┘
```

---

## 3. 사용 기술 및 설계

### 🛠 기술 스택

#### Backend
| 기술 | 버전 | 용도 |
|------|------|------|
| Python | 3.12 | 메인 언어 |
| Django | 5.2 | 웹 프레임워크 |
| Django REST Framework | 3.16 | REST API |
| SQLite | 3 | 데이터베이스 |

#### AI/ML
| 기술 | 용도 |
|------|------|
| **GPT-5 Nano** | AI 게임 큐레이터 챗봇 |
| **Gemini 2.0 Flash Lite** | 실시간 한국어 번역 |
| **SciPy** | 코사인 유사도 계산 |
| **Scikit-learn** | 희소 행렬 처리 |
| **Pandas/NumPy** | 데이터 분석 및 처리 |

#### Frontend
| 기술 | 용도 |
|------|------|
| Vue.js 3.0 | SPA 인터랙션 |
| HTML5/CSS3 | 기본 구조 및 스타일 |
| JavaScript | 동적 기능 구현 |

#### External APIs
| API | 용도 |
|-----|------|
| Steam Web API | 사용자 라이브러리, 프로필 |
| Steam OpenID 2.0 | 소셜 로그인 |
| RAWG Game API | 게임 메타데이터 |
| CheapShark API | 세일 정보 |
| SSAFY GMS API | AI 모델 호출 |

### 📐 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                         Client (Browser)                         │
│                    Vue.js SPA + Django Templates                 │
└──────────────────────────────┬──────────────────────────────────┘
                               │ REST API
┌──────────────────────────────▼──────────────────────────────────┐
│                     Django REST Framework                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐   │
│  │  Steam Auth  │  │ Recommender  │  │   AI Chat (GPT)      │   │
│  │  (OpenID)    │  │   Engine     │  │   Translate (Gemini) │   │
│  └──────┬───────┘  └──────┬───────┘  └──────────┬───────────┘   │
└─────────┼─────────────────┼─────────────────────┼───────────────┘
          │                 │                     │
┌─────────▼─────────────────▼─────────────────────▼───────────────┐
│                        SQLite Database                           │
│  ┌──────────┐  ┌───────────┐  ┌────────────────────────────┐    │
│  │   User   │  │   Game    │  │    GameSimilarity          │    │
│  │  Rating  │  │   Tag     │  │    (Pre-calculated CF)     │    │
│  │  Wishlist│  │   Cache   │  │    SteamLibraryCache       │    │
│  └──────────┘  └───────────┘  └────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
          │                 │                     │
┌─────────▼─────────────────▼─────────────────────▼───────────────┐
│                      External APIs                               │
│  ┌──────────┐  ┌───────────┐  ┌───────────┐  ┌───────────────┐  │
│  │ Steam API│  │ RAWG API  │  │CheapShark │  │ SSAFY GMS     │  │
│  │ (Library)│  │ (Metadata)│  │  (Sale)   │  │ (GPT/Gemini)  │  │
│  └──────────┘  └───────────┘  └───────────┘  └───────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. AI 기능 상세

### 🤖 AI 기능 1: 게임 추천 챗봇 (GPT-5 Nano)

#### 개요
사용자의 게임 취향을 이해하고 개인화된 추천을 제공하는 AI 큐레이터

#### 구현 방식

**Context-Aware 추천 시스템:**
1. **사용자 데이터 수집**: Steam 라이브러리 + 온보딩 평가
2. **프롬프트 인젝션**: 개인화된 컨텍스트를 시스템 프롬프트에 주입
3. **동적 추천**: 대화 히스토리 기반 맥락 파악

#### 사용 프롬프트

```python
# users/views.py - ai_chat_api 함수 (Line 689-721)

system_prompt_text = f"""당신은 '게임 큐레이터 AI'입니다. 게임 추천 전문가로서 다음 역할을 수행합니다:

🎮 **전문 분야**
- 모든 플랫폼(PC, 콘솔, 모바일)의 게임에 대한 깊은 지식
- 장르별 특성과 대표 게임들을 잘 알고 있음
- 최신 인기 게임과 숨겨진 명작까지 폭넓게 추천 가능

📊 **추천 스타일**
- 유저의 취향과 플레이 스타일을 파악하여 맞춤 추천
- 게임의 장점, 특징, 플레이 시간, 난이도 등을 설명
- 이모지를 활용하여 친근하고 재미있게 대화

🚫 **중요: 추천 규칙**
1. 유저가 이미 평가하거나 보유한 게임은 새 게임 추천에서 **반드시 제외**
2. 추천할 때 반드시 유저가 플레이/평가한 게임과 비교하며 설명:
   - "'{user_nickname}님이 좋아하신 OO 게임처럼 △△한 요소가 있어서..."
3. 유저의 선호 장르와 좋아하는 게임의 공통점을 분석해서 구체적으로 설명
4. 유저가 싫어한 게임과 비슷한 장르/스타일은 피해주기

{onboarding_context}
{steam_context}
"""
```

**동적 컨텍스트 생성 예시:**
```python
# 평가 데이터 기반 컨텍스트
onboarding_context = f"""
[평가 데이터]
- 선호 장르: {', '.join(top_genres)}
- 좋아한 게임: {', '.join(liked_games[:7])}
- 싫어한 게임: {', '.join(disliked_games[:5])}
"""

# Steam 라이브러리 기반 컨텍스트
steam_context = f"""
[Steam 라이브러리]
- 최다 플레이: {', '.join(top_played)}
- 보유 게임 수: {len(steam_library)}개
"""
```

#### API 호출 코드

```python
# Gemini Native API 호출 (Line 742-777)
payload = {
    "systemInstruction": {
        "parts": [{"text": system_prompt_text}]
    },
    "contents": gemini_contents,  # 대화 히스토리
    "generationConfig": {
        "temperature": 0.7,
        "maxOutputTokens": 2048,
        "topP": 0.8,
        "topK": 40
    }
}

url = "https://gms.ssafy.io/gmsapi/generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-lite:generateContent"

response = requests.post(
    url,
    params={'key': api_key},
    headers={'Content-Type': 'application/json'},
    json=payload,
    timeout=30
)
```

---

### 🌐 AI 기능 2: 실시간 번역 (Gemini 2.0 Flash Lite)

#### 개요
영어 게임 설명을 게이머 친화적인 한국어로 번역

#### 핵심 포인트
- **전문 번역가 페르소나**: 10년 경력 게임 로컬라이제이션 전문가
- **게임 용어 보존**: 고유명사, 게임 용어는 현지화된 표현 사용
- **자연스러운 의역**: 번역투 최소화

#### 사용 프롬프트

```python
# users/views.py - translate_text_api 함수 (Line 859-891)

prompt = f"""당신은 10년 경력의 전문 게임 로컬라이제이션 번역가입니다. 

📜 **번역 원칙:**
1. **고유명사 보존**: 게임 타이틀, 캐릭터명은 원어 그대로 유지
   - 예: "Dark Souls" → "Dark Souls" (게임 타이틀은 그대로)

2. **게임 용어 현지화**: 한국 게이머들에게 익숙한 표현 사용
   - 예: "roguelike" → "로그라이크", "open world" → "오픈 월드"

3. **자연스러운 한국어**: 직역 금지, 의역을 통해 매끄럽게

4. **마케팅 톤 유지**: 원문의 흥미와 기대감을 살려서 번역

5. **출력 규칙**: 오직 번역된 텍스트만 출력

---
영어 원문:
{text}

한국어 번역:"""
```

#### 번역 API 호출

```python
# games/utils.py - translate_text_gemini 함수 (Line 188-233)

response = requests.post(
    f"https://gms.ssafy.io/gmsapi/generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash-lite:generateContent?key={api_key}",
    headers={"Content-Type": "application/json"},
    json={
        "contents": [{"parts": [{"text": prompt}]}]
    },
    timeout=30
)
```

---

## 5. 구현 내용

### 📁 프로젝트 구조

```
ChuraiGame/
├── ChuraiGame/              # Django 프로젝트 설정
│   ├── settings.py          # 환경 설정
│   ├── urls.py               # URL 라우팅
│   └── wsgi.py
│
├── users/                    # 사용자 관리 앱
│   ├── models.py             # User, GameRating, GameSimilarity 등
│   ├── views.py              # 메인 뷰, AI 챗봇, 번역 API (96KB)
│   ├── steam_auth.py         # Steam OpenID 로그인
│   ├── recommendation.py     # Steam 기반 추천 알고리즘
│   ├── onboarding.py         # 왓챠 스타일 평가 + Item-Based CF
│   ├── hybrid_similarity.py  # 하이브리드 추천 (CF + Content)
│   └── management/commands/  # 배치 작업
│       ├── calculate_game_similarity.py
│       ├── update_steam_sales.py
│       └── add_korean_games.py
│
├── games/                    # 게임 데이터 앱
│   ├── models.py             # Game, Tag, CachedGameList 등
│   ├── views.py              # 게임 상세, API
│   ├── utils.py              # RAWG API 래퍼, 번역
│   └── management/commands/  # 데이터 관리
│       ├── load_games.py
│       ├── refresh_game_cache.py
│       └── fetch_steam_reviews.py
│
├── community/                # 커뮤니티 앱
│   ├── models.py             # Post, Comment
│   └── views.py
│
├── templates/                # Django 템플릿
└── requirements.txt          # 의존성 목록
```

### ⚡ 주요 기능 구현

#### 1. Steam OpenID 로그인 연동

```python
# users/steam_auth.py

def get_steam_login_url(return_to_url):
    """Steam OpenID 로그인 URL 생성"""
    params = {
        'openid.ns': 'http://specs.openid.net/auth/2.0',
        'openid.mode': 'checkid_setup',
        'openid.return_to': return_to_url,
        'openid.realm': return_to_url.split('/users/')[0] + '/',
        'openid.identity': 'http://specs.openid.net/auth/2.0/identifier_select',
        'openid.claimed_id': 'http://specs.openid.net/auth/2.0/identifier_select',
    }
    return f"{STEAM_OPENID_URL}?{urlencode(params)}"

def validate_steam_login(params):
    """Steam 로그인 응답 검증"""
    validation_params = {...}
    response = requests.post(STEAM_OPENID_URL, data=validation_params)
    if 'is_valid:true' in response.text:
        # Steam ID 64비트 추출
        match = re.search(r'https://steamcommunity\.com/openid/id/(\d+)', claimed_id)
        return match.group(1)
    return None
```

#### 2. 왓챠 스타일 온보딩

```python
# users/onboarding.py

RATING_CHOICES = [
    (-1, '역따봉'),   # 싫어요
    (0, '스킵'),      # 관심없음
    (3.5, '따봉'),    # 좋아요
    (5, '쌍따봉'),    # 인생게임
]

def get_onboarding_games(step=0, exclude_rated=None, page=1, per_page=8):
    """온보딩 게임 목록 반환 (페이지네이션)"""
    # Steam 평점 75% 이상, 리뷰 500개 이상 필터링
    quality_games = [g for g in data 
        if g.get('steam_rating', 0) >= 75 
        and g.get('review_count', 0) >= 500]
    # 리뷰 수 기준 정렬 (인기 게임)
    sorted_games = sorted(quality_games, 
        key=lambda x: x.get('review_count', 0), reverse=True)
    return paginated_games
```

#### 3. DB 캐싱 시스템

```python
# games/models.py

class CachedGameList(models.Model):
    """RAWG API 응답 캐싱 (6시간 TTL)"""
    CATEGORY_CHOICES = [
        ('popular', '요즘 뜨는'),
        ('top_rated', '평점 높은'),
        ('new_releases', '신작 게임'),
    ]
    
    category = models.CharField(max_length=20, unique=True)
    games_data = models.JSONField(default=list)
    updated_at = models.DateTimeField(auto_now=True)
    
    @classmethod
    def get_cached_games(cls, category, max_age_hours=6):
        """캐시 조회 (TTL 체크)"""
        cache = cls.objects.get(category=category)
        if age < timedelta(hours=max_age_hours):
            return cache.games_data  # 캐시 히트!
        return None  # 캐시 미스 → API 호출
```

---

## 6. 주요 코드 및 알고리즘

### 📊 알고리즘 1: Item-Based Collaborative Filtering

**위치:** `users/onboarding.py` (Line 329-460)

```python
def calculate_game_similarity_batch(min_ratings=1, top_k=50, min_similarity=0.1):
    """
    배치 작업: 게임 간 유사도 계산
    - 희소 행렬로 메모리 효율화 (SciPy csr_matrix)
    - 코사인 유사도 계산 (sklearn cosine_similarity)
    - 정규화된 스키마: game_a_id < game_b_id (저장 공간 50% 절약)
    """
    
    # 1. 평점 정규화 (비선형 → 선형)
    SCORE_NORMALIZATION = {-1: -1.0, 0: 0.0, 3.5: 0.7, 5: 1.0}
    
    # 2. 희소 행렬 생성 (게임 x 유저)
    sparse_matrix = csr_matrix(
        (scores, (game_codes, user_codes)),
        shape=(num_games, num_users)
    )
    
    # 3. 코사인 유사도 계산
    similarity_matrix = cosine_similarity(sparse_matrix)
    
    # 4. Top-K 유사 게임만 저장 (DB 최적화)
    for i, game_id in enumerate(game_ids):
        sorted_indices = np.argsort(similarity_matrix[i])[::-1]
        for rank, j in enumerate(sorted_indices[:top_k]):
            if similarity_matrix[i][j] >= min_similarity:
                # 정규화: 항상 작은 ID를 game_a로
                game_a_id = min(game_id, other_id)
                game_b_id = max(game_id, other_id)
                GameSimilarity.objects.create(
                    game_a_id=game_a_id,
                    game_b_id=game_b_id,
                    similarity_score=score,
                    similarity_rank=rank
                )
```

**핵심 포인트:**
- `csr_matrix`: 희소 행렬로 메모리 효율 극대화
- 정규화된 저장: `(A,B)` = `(B,A)` → 중복 제거로 50% 공간 절약
- Top-K 필터링: 유사도 낮은 쌍은 저장하지 않음

---

### 📊 알고리즘 2: 하이브리드 추천 점수 계산

**위치:** `users/recommendation.py` (Line 242-279)

```python
def calculate_recommendation_score(game, user_genres, is_on_sale=False, sale_discount=0):
    """
    추천 점수 계산 (0-100)
    
    가중치 배분:
    1. 장르 매칭: 40점 (가장 중요)
    2. 메타크리틱: 25점 (품질 보장)
    3. 유저 평점: 20점
    4. 할인율: 15점 (부가 요소)
    """
    score = 0
    
    # 1. 장르 매칭 점수 (40점 만점)
    game_genres = [g.lower() for g in game.get('genres', [])]
    genre_matches = sum(user_genres.get(g, 0) for g in game_genres)
    genre_score = min(40, (genre_matches / max_genre_score) * 40)
    score += genre_score
    
    # 2. 메타크리틱 점수 (25점 만점)
    metacritic = game.get('metacritic') or 0
    if metacritic > 0:
        # 60-100점 범위를 0-25점으로 스케일링
        metacritic_score = min(25, max(0, (metacritic - 60) / 40 * 25))
        score += metacritic_score
    
    # 3. 유저 평점 (20점 만점)
    rating = game.get('rating', 0) or 0
    rating_score = (rating / 5) * 20
    score += rating_score
    
    # 4. 할인 보너스 (15점 만점)
    if is_on_sale:
        sale_score = min(15, (sale_discount / 100) * 15)
        score += sale_score
    
    return round(score, 1)
```

---

### 📊 알고리즘 3: 하이브리드 유사도 계산

**위치:** `users/hybrid_similarity.py` (Line 132-176)

```python
# 가중치 설정
SIMILARITY_WEIGHTS = {
    'collaborative': 0.70,   # 협업 필터링 (가장 중요)
    'genre': 0.20,           # 장르/태그 유사도
    'metacritic': 0.10,      # 메타크리틱 점수 유사도
}

def calculate_hybrid_similarity(game_a, game_b, weights=None):
    """
    하이브리드 유사도 = 가중합
    
    final_similarity = 
        0.70 * collaborative_similarity +
        0.20 * genre_similarity +
        0.10 * metacritic_similarity
    """
    
    # 1. 협업 필터링 유사도 (GameSimilarity 테이블 조회)
    collab_sim = get_collaborative_similarity(game_a.id, game_b.id)
    
    # 2. 장르/태그 유사도 (Jaccard Index)
    genre_sim = calculate_genre_similarity(game_a, game_b)
    
    # 3. 메타크리틱 유사도 (점수 차이 기반)
    meta_sim = calculate_metacritic_similarity(
        game_a.metacritic_score, 
        game_b.metacritic_score
    )
    
    # 가중합 계산
    final_similarity = (
        0.70 * collab_sim +
        0.20 * genre_sim +
        0.10 * meta_sim
    )
    
    return final_similarity
```

---

### 📊 알고리즘 4: 장르 유사도 (Jaccard Index)

```python
def calculate_genre_similarity(game_a, game_b):
    """
    장르/태그 유사도 계산 (Jaccard Index)
    
    공식: |A ∩ B| / |A ∪ B|
    """
    tags_a = set(game_a.tags.values_list('slug', flat=True))
    tags_b = set(game_b.tags.values_list('slug', flat=True))
    
    intersection = len(tags_a & tags_b)  # 교집합
    union = len(tags_a | tags_b)         # 합집합
    
    return intersection / union if union > 0 else 0.0
```

---

## 7. ERD 관계

### 📊 데이터베이스 스키마

![ERD](erd.png)

### 주요 테이블 관계

```
┌─────────────────────────────────────────────────────────────────┐
│                          User (사용자)                           │
│  - id, username, email, password                                │
│  - steam_id (Steam 연동)                                        │
│  - is_steam_linked, is_naver_linked, is_google_linked           │
│  - wishlist (M:N → Game)                                        │
└─────────────────────────┬───────────────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        ▼                 ▼                 ▼
┌───────────────┐ ┌───────────────┐ ┌──────────────────┐
│  GameRating   │ │OnboardingStatus│ │SteamLibraryCache│
│ (사용자 평가)  │ │ (온보딩 상태)  │ │ (라이브러리 캐시)│
│---------------│ │---------------│ │------------------│
│ user (FK)     │ │ user (1:1)    │ │ user (1:1)       │
│ game (FK)     │ │ status        │ │ library_data JSON│
│ score (-1~5)  │ │ current_step  │ │ total_games      │
│ is_onboarding │ │ total_ratings │ │ last_updated     │
└───────────────┘ └───────────────┘ └──────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────┐
│                          Game (게임)                             │
│  - id, steam_appid, rawg_id                                     │
│  - title, genre, description, description_kr                    │
│  - image_url, background_image                                  │
│  - metacritic_score, is_on_gamepass                             │
│  - tags (M:N → Tag)                                             │
└─────────────────────────┬───────────────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┬────────────────┐
        ▼                 ▼                 ▼                ▼
┌───────────────┐ ┌───────────────┐ ┌──────────────┐ ┌──────────────┐
│     Tag       │ │GameScreenshot │ │ GameTrailer  │ │ SteamReview  │
│ (태그/장르)   │ │ (스크린샷)    │ │  (트레일러)  │ │ (Steam 리뷰)│
│---------------│ │---------------│ │--------------│ │--------------│
│ name, slug    │ │ game (FK)     │ │ game (FK)    │ │ game (FK)    │
│ tag_type      │ │ image_url     │ │ name         │ │ content      │
│ weight        │ │               │ │ data_480/max │ │ is_recommended│
└───────────────┘ └───────────────┘ └──────────────┘ └──────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    GameSimilarity (게임 유사도)                  │
│  - game_a (FK) - 항상 game_b보다 작은 ID                        │
│  - game_b (FK) - 항상 game_a보다 큰 ID                          │
│  - similarity_score (0~1)                                        │
│  - similarity_rank (Top-K 최적화용)                              │
│                                                                  │
│  ※ 정규화 규칙: game_a_id < game_b_id (저장 공간 50% 절약)      │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                     Community (커뮤니티)                         │
├─────────────────────────┬───────────────────────────────────────┤
│         Post            │              Comment                   │
│ (게시글)                │              (댓글)                    │
│-------------------------│---------------------------------------│
│ author (FK → User)      │ post (FK → Post)                      │
│ category (리뷰/공략/자유)│ author (FK → User)                    │
│ title, content          │ content                               │
│ image, video            │ like_users (M:N)                      │
│ like_users (M:N)        │                                       │
└─────────────────────────┴───────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                  CachedGameList (게임 목록 캐시)                 │
│  - category (popular/top_rated/new_releases/trending/upcoming) │
│  - games_data (JSON)                                            │
│  - updated_at (TTL 체크용)                                      │
│                                                                  │
│  ※ RAWG API 캐싱: 로딩 시간 9초 → 0.15초 (98% 단축)            │
└─────────────────────────────────────────────────────────────────┘
```

### 테이블 상세 설명

| 테이블 | 역할 | 특이사항 |
|--------|------|----------|
| **User** | 사용자 정보 | AbstractUser 상속, Steam/Naver/Google 연동 |
| **GameRating** | 게임 평가 | 왓챠 스타일 (-1, 0, 3.5, 5점) |
| **GameSimilarity** | 게임 간 유사도 | 정규화 저장, Top-K 랭크 인덱싱 |
| **SteamLibraryCache** | Steam 라이브러리 캐시 | 24시간 TTL, JSON 저장 |
| **CachedGameList** | RAWG API 캐시 | 6시간 TTL, 카테고리별 |
| **Tag** | 태그/장르 | 가중치 지원, 유형 분류 |
| **SteamReview** | Steam 리뷰 크롤링 | 한국어 리뷰만 수집 |

---

## 8. 어려웠던 점 및 해결 과정

### 🔥 Challenge 1: RAWG API 속도 문제

```
┌────────────────────────────────────────────────────────────────┐
│  📍 문제 상황                                                   │
│  메인 페이지 로딩 시 Popular, Trending, New Release 등          │
│  5개 섹션 × 20개 게임 = 매번 100개 API 호출                    │
│  → 로딩 시간 9초 이상!                                         │
├────────────────────────────────────────────────────────────────┤
│  💡 해결 방법: DB Caching Layer                                │
│                                                                 │
│  1. CachedGameList 모델 생성                                   │
│  2. 카테고리별 JSON으로 게임 목록 저장                          │
│  3. TTL 6시간 설정 (Time-To-Live)                              │
│  4. 캐시 미스 시에만 RAWG API 호출                              │
├────────────────────────────────────────────────────────────────┤
│  ✅ 결과                                                        │
│  9초 → 0.15초 (약 98% 속도 향상!)                              │
└────────────────────────────────────────────────────────────────┘
```

**구현 코드:**
```python
# games/models.py
class CachedGameList(models.Model):
    @classmethod
    def get_cached_games(cls, category, max_age_hours=6):
        try:
            cache = cls.objects.get(category=category)
            age = timezone.now() - cache.updated_at
            if age < timedelta(hours=max_age_hours):
                return cache.games_data  # ✅ 캐시 히트!
        except cls.DoesNotExist:
            pass
        return None  # ❌ 캐시 미스 → API 호출
```

---

### 🔥 Challenge 2: Cold Start 문제

```
┌────────────────────────────────────────────────────────────────┐
│  📍 문제 상황                                                   │
│  신규 가입자는 평가 데이터가 없음                               │
│  → 협업 필터링 추천 불가능!                                     │
├────────────────────────────────────────────────────────────────┤
│  💡 해결 방법: 왓챠 스타일 온보딩                               │
│                                                                 │
│  1. 가입 직후 인기 게임 1,500개 표시                            │
│  2. 스와이프/클릭으로 빠른 평가                                 │
│     - 역따봉(-1): 싫어요                                       │
│     - 스킵(0): 모르겠음                                        │
│     - 따봉(3.5): 좋아요                                        │
│     - 쌍따봉(5): 인생게임                                      │
│  3. 최소 3개 평가 시 즉시 추천 작동                             │
├────────────────────────────────────────────────────────────────┤
│  ✅ 결과                                                        │
│  신규 유저도 30초 내에 개인화 추천 경험!                        │
└────────────────────────────────────────────────────────────────┘
```

---

### 🔥 Challenge 3: GameSimilarity 저장 공간 문제

```
┌────────────────────────────────────────────────────────────────┐
│  📍 문제 상황                                                   │
│  N개 게임의 유사도 = N² 개 레코드                               │
│  10,000개 게임 → 1억 개 레코드!                                 │
├────────────────────────────────────────────────────────────────┤
│  💡 해결 방법: 정규화 + Top-K 필터링                            │
│                                                                 │
│  1. 정규화 규칙: game_a_id < game_b_id                          │
│     - (A, B, 0.8)과 (B, A, 0.8) 중복 제거                      │
│     - 저장 공간 50% 절약                                       │
│                                                                 │
│  2. Top-K 필터링: 상위 50개 유사 게임만 저장                    │
│     - 유사도 낮은 쌍은 버림                                    │
│     - 실제 저장: N × 50 = 50만 개 레코드                       │
├────────────────────────────────────────────────────────────────┤
│  ✅ 결과                                                        │
│  1억 개 → 50만 개 (99.5% 절약!)                                │
└────────────────────────────────────────────────────────────────┘
```

**정규화 코드:**
```python
@staticmethod
def normalize_game_ids(game_x_id, game_y_id):
    """항상 game_a_id < game_b_id 규칙 적용"""
    return (min(game_x_id, game_y_id), max(game_x_id, game_y_id))
```

---

### 🔥 Challenge 4: Steam 라이브러리 API 호출 속도

```
┌────────────────────────────────────────────────────────────────┐
│  📍 문제 상황                                                   │
│  Steam API 호출 시 2-3초 소요                                   │
│  매번 페이지 접속할 때마다 느린 로딩                            │
├────────────────────────────────────────────────────────────────┤
│  💡 해결 방법: SteamLibraryCache 모델                           │
│                                                                 │
│  1. 첫 로딩 시 Steam API 호출 → DB에 JSON 저장                  │
│  2. 이후 요청은 DB에서 즉시 반환 (0.01초)                       │
│  3. 24시간마다 백그라운드 업데이트                              │
├────────────────────────────────────────────────────────────────┤
│  ✅ 결과                                                        │
│  2초 → 0.01초 (99% 속도 향상!)                                 │
└────────────────────────────────────────────────────────────────┘
```

---

### 🔥 Challenge 5: 추천 알고리즘 정확도

```
┌────────────────────────────────────────────────────────────────┐
│  📍 문제 상황                                                   │
│  단순 장르 매칭만으로는 추천 정확도 낮음                        │
│  "액션 좋아함" → 모든 액션 게임 추천?                           │
├────────────────────────────────────────────────────────────────┤
│  💡 해결 방법: 하이브리드 추천                                  │
│                                                                 │
│  최종 점수 = 가중합                                             │
│  - 장르 적합도: 40%                                            │
│  - 메타크리틱 점수: 25%                                        │
│  - 유저 평점: 20%                                              │
│  - 할인율: 15%                                                 │
├────────────────────────────────────────────────────────────────┤
│  ✅ 결과                                                        │
│  품질 높은 게임 + 취향 맞는 게임 우선 추천!                     │
└────────────────────────────────────────────────────────────────┘
```

---

## 9. 역할 분담 및 일정

### 👥 팀 구성

| 역할 | 담당 업무 |
|------|----------|
| **Backend 개발** | Django REST Framework, 추천 알고리즘, AI 연동 |
| **Frontend 개발** | Vue.js SPA, UI/UX 디자인 |
| **Data Engineering** | Steam/RAWG API 연동, 데이터 전처리 |
| **DevOps** | 배포, DB 관리, 성능 최적화 |

### 📅 개발 일정

```
Week 1: 기획 및 설계
├── 요구사항 분석
├── ERD 설계
├── API 스펙 정의
└── 기술 스택 선정

Week 2: 핵심 기능 개발
├── Steam OpenID 로그인
├── 게임 데이터 적재 (JSON → DB)
├── RAWG API 연동
└── 온보딩 시스템

Week 3: 추천 알고리즘 개발
├── Item-Based CF 구현
├── 하이브리드 추천
├── GameSimilarity 배치 작업
└── 캐싱 시스템

Week 4: AI 기능 개발
├── GPT 챗봇 연동
├── Gemini 번역 기능
├── 프롬프트 엔지니어링
└── 컨텍스트 주입

Week 5: 마무리
├── 버그 수정
├── 성능 최적화
├── 문서화
└── 발표 준비
```

---

## 10. 시연

### 🎬 시연 항목

#### 1. Steam 로그인 연동
- Steam OpenID로 로그인
- 자동으로 라이브러리 동기화
- 플레이타임 순으로 게임 표시

#### 2. 온보딩 (왓챠 스타일)
- 인기 게임 스와이프 평가
- 따봉/역따봉/스킵 선택
- 3개 이상 평가 시 추천 활성화

#### 3. AI 게임 추천 챗봇
- "액션 RPG 추천해줘"
- "엘든링 같은 게임 있어?"
- 사용자 취향 기반 개인화 응답

#### 4. 실시간 번역
- 영어 게임 설명 클릭
- Gemini로 1초 내 한국어 번역
- 게임 용어 자연스럽게 현지화

#### 5. 개인화 추천
- "나를 위한 추천" 섹션
- Steam 라이브러리 + 평가 데이터 기반
- 장르/메타크리틱/할인율 종합 점수

### 💻 시연 스크립트

```
1. 메인 페이지 접속 (캐시로 0.15초 로딩)
2. Steam 로그인 버튼 클릭
3. Steam 인증 후 자동 리다이렉트
4. "내 라이브러리" 탭에서 보유 게임 확인
5. "나를 위한 추천" 섹션 확인
6. AI 큐레이터 챗봇 열기
7. "내 스팀 라이브러리 보고 추천해줘" 입력
8. AI가 플레이타임 분석 후 맞춤 추천
9. 추천 게임 상세 페이지 이동
10. "번역하기" 버튼으로 한국어 설명 확인
```

---

## 📚 참고 자료

### API 문서
- [Steam Web API](https://steamcommunity.com/dev)
- [RAWG Games API](https://rawg.io/apidocs)
- [CheapShark API](https://apidocs.cheapshark.com/)
- [Google Gemini API](https://ai.google.dev/)

### 사용 라이브러리
```
Django==5.2.8
djangorestframework==3.16.1
scikit-learn==1.8.0
scipy==1.16.3
numpy==2.3.5
pandas==2.3.3
requests==2.32.5
python-dotenv==1.2.1
```

---

<div align="center">

**SSAFY 14기 1학기 관통 프로젝트**

🎮 GameMatch - 당신의 취향, AI가 찾아드립니다. 🎮

</div>
